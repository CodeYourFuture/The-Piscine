{
  "name": "project-spell-checker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d ."
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}